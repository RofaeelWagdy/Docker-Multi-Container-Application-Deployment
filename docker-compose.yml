services:
  my_database:
    build: ./database
    restart: always
    ports:
      - "3306:3306"
    volumes:
      - mysql-vol:/var/lib/mysql
    environment:
      - MYSQL_ROOT_PASSWORD=root
      - MYSQL_DATABASE=user_db
  
  my_backend:
    depends_on:
      - my_database
    build: ./backend
    restart: always
    ports:
      - "3000:3000"
    volumes:
      - ~/Downloads/:/app/data/
    environment:
      - DB_HOST=my_database
      - DB_USER=root
      - DB_PASSWORD=root
      - DB_NAME=user_db

  my_frontend:
    depends_on:
      - my_backend
      - my_database
    build: ./frontend
    ports:
      - "5173:5173"

volumes:
  mysql-vol: {}